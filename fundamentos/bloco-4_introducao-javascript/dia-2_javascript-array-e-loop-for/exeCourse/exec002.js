let groceryList = ['Arroz', 'Feijão', 'Alface', 'Melancia'];
for (var indice = 0; indice < groceryList.length; indice += 1) {
  console.log(groceryList[indice])
}

// Utilizando o for/of
let names = ['João', 'Maria', 'Antônio', 'Margarida'];
for (let nome of names) {
  console.log(nome);
}
